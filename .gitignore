.*
!/.gitignore

# local GitHub token (never commit)
/.github-token
/.git-credentials

# local dev server config (browser-sync extensionless middleware)
/bs-config.js

# local CV scratch (served copies live at repo root)
/docs/

# personal copy guide - keep local, never publish (Pages serves every tracked file)
/WRITING_STYLE.md